# 🧠 Personal Portfolio Backend (Node.js + Express + MongoDB)

This is the **backend** for the personal portfolio website built using  
**Node.js**, **Express.js**, and **MongoDB**.

It serves RESTful APIs that the React frontend consumes dynamically.

---

## 🚀 Features

- REST API endpoints for dynamic content
- Separate routes for each page:
  - `/api/home` – Fetch Home page content
  - `/api/about` – Fetch About page content
  - `/api/contact` – Fetch Contact page content and handle form submissions
- MongoDB-based content management (Mongoose schemas)
- JSON-based data fetching (frontend consumes via fetch)
- CORS-enabled for local React connection

---
